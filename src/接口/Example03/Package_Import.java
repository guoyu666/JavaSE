package 接口.Example03;

/**
 * 1.package怎么用？
 *      --package是一个关键字，后面加包名，且package语句只允许出现在java源代码的第一行
 *
 * 2.包名的命名规范？
 *      --一般都采用公司域名倒序的方式（因为公司域名具有全球唯一性）
 *
 * 3.关于import的使用
 *      --什么时候使用？--A类中使用B类，且A类和B类都在同一个包下，不需要import。A和B类不在同一个包下，需要使用import
 *      --还可以使用带有星号的方式
 */
public class Package_Import {
}
